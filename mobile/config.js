// Backend address the app connects to. During the demo the backend runs locally
// and is exposed through ngrok, so paste that https URL here. Using the ngrok
// URL (instead of a LAN IP) means the phone reaches it on any network.
export const BACKEND_URL = 'https://YOUR-NGROK-SUBDOMAIN.ngrok-free.app';
